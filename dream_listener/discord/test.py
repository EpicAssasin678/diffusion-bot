

""" class DiffusionBot(discord.Bot):
    async def on_ready(self):
        print(f'Logged in as {self.user.name}#{self.user.id}')

    
    async def ping(self, ctx):
        await ctx.respond('I am alive!!! But now I\'m running an ai...*gulp* totally a natural at this!')



 """